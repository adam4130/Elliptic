#include "babygiant.h"

#include <unordered_map>

/*
 * Compute the discrete logarithm on an elliptic curve.
 */
elliptic::Point elliptic::babyGiant(const Curve& c, const mpz_class& n) {
    return elliptic::Point();
}
